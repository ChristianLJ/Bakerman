#!/usr/bin/env node

const bakerman = require('../_start');

'use strict';

let mode = "PROD";

if(process.argv.indexOf("--dev") > -1) {
    mode = "DEV";
}
if(process.argv.indexOf("--prod") > -1) {
    mode = "PROD";
}

bakerman.start(mode);